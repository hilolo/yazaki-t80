import { TypereformatPipe } from './typereformat.pipe';

describe('TypereformatPipe', () => {
  it('create an instance', () => {
    const pipe = new TypereformatPipe();
    expect(pipe).toBeTruthy();
  });
});
